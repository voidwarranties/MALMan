client side form validation

add transaction DB

make accounting forms

translate everything to english

use "return redirect" afther proccesing POSTs to prevent data from being submitted twice if users refresh the page

Use wtforms for all forms

return a 401 status if not allowed
	    return render_template('page_not_found.html'), 404