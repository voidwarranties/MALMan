Funtionality
	add transactions DB
	make accounting forms
	filtering of tables
	pagination of tables

Refactoring
	translate everything to english
	clean up forms:
		reddirect after a POST (to prevent data from being submitted twice if users refresh the page)
		use wtforms
		add necessary validators
		form that need to be done:
			stock_tellen
			stock_aanpassen
			stock_aanvullen
			stock_toevoegen
	remove unused code from flask_security/ (~2000 of ~3000 LOC are in here)

Finishing touches
	Minimize the number of times we read or write to the DB
	check validity of XHTML, CSS & JS
	have template produce beautiful XHTML
	improve graphical design
