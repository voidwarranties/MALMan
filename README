dependencies
============


to run MALMan
=============

import database.sql
copy MALMan.cfg.template to MALMan.cfg and adapt the file to fit your setup
execute run.py
MALMan should be running on 0.0.0.0:5000
by default you can log in with the following credentials: 
	user: root@vw.be
	password: password